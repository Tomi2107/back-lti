# ─── App ─────────────────────────────────────────────────────────────────────
NODE_ENV=development
PORT=3000
APP_URL=https://your-backend.example.com

# ─── Frontend (equipo de front) ───────────────────────────────────────────────
# URL base de la app del equipo de front (sin trailing slash).
# El backend redirige aquí después de autenticar, enviando el JWT como ?token=
FRONTEND_URL=https://your-frontend.example.com

# ─── Seguridad ───────────────────────────────────────────────────────────────
# Clave de cifrado para ltijs — mínimo 32 chars aleatorios
# Genera con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
LTI_KEY=7ec77c722315d78fd603556555e6a83ea434299dea654de1c5d46741689ca4e8

# ─── Base de datos — PostgreSQL ───────────────────────────────────────────────
# URL completa usada por Prisma (migraciones y queries de la app)
DATABASE_URL=postgresql://user:password@localhost:5432/lti_accessibility

# Credenciales individuales usadas por ltijs-sequelize (tablas internas de LTI)
PG_HOST=localhost
PG_PORT=5432
PG_DATABASE=lti_accessibility
PG_USER=user
PG_PASSWORD=password

# ─── Plataforma LMS (Canvas, Moodle, etc.) ───────────────────────────────────
PLATFORM_URL=https://lms.example.com
PLATFORM_NAME=Mi LMS
PLATFORM_CLIENT_ID=client_id

# Canvas: /api/lti/authorize_redirect
# Moodle: /mod/lti/auth.php
PLATFORM_AUTH_ENDPOINT=https://lms.example.com/api/lti/authorize_redirect

# Canvas: /login/oauth2/token
# Moodle: /mod/lti/token.php
PLATFORM_TOKEN_ENDPOINT=https://lms.example.com/login/oauth2/token

# Canvas: /api/lti/security/jwks
# Moodle: /mod/lti/certs.php
PLATFORM_KEYSET_ENDPOINT=https://lms.example.com/api/lti/security/jwks

# ─── CORS ────────────────────────────────────────────────────────────────────
ALLOWED_ORIGINS=http://localhost:5173,https://your-frontend.example.com

# ─── Botón flotante (flujo no-LTI) ───────────────────────────────────────────
# Debe coincidir con el "Secreto compartido" configurado en Moodle → Plugin de accesibilidad
# Genera con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MOODLE_SHARED_SECRET=replace_with_random_secret

# ─── Moodle REST API ──────────────────────────────────────────────────────────
# Token de un servicio web Moodle con permisos:
#   core_course_get_contents, core_completion_get_activities_completion_status
MOODLE_SERVICE_TOKEN=

# ─── IA (Anthropic) ──────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=
